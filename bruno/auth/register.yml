info:
  name: register
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/register"
  body:
    type: json
    data: |-
      {
        "name": "Rian Ardianto",
        "email": "rian.student@soalhub.com",
        "password": "password123",
        "role": "student"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
