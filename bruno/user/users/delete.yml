info:
  name: delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/users/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
